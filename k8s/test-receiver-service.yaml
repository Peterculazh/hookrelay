apiVersion: v1
kind: Service
metadata:
  name: test-receiver
  namespace: hookrelay
spec:
  type: ClusterIP
  selector:
    app: test-receiver
  ports:
    - name: http
      port: 3001
      targetPort: http
